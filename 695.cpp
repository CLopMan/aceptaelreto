/*https://aceptaelreto.com/problem/statement.php?id=695*/
#include <iostream>
#include <vector> 

void tratar_caso(uint32_t num) {
    std::vector<int> v_min; 
    std::vector<int> v_max;
}

int main() {
    uint32_t num; 
    std:: cin >> num; 
    while (num > 0) {
        tratar_caso(num);
        std::cin >> num; 
    }
    return 0;
}